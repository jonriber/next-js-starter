import CustomButton from "@/app/components/CustomButton"

const AboutInside = () => {
    return(
        <div>
            <h2>Nested inside and consuming a custom component</h2>
            <CustomButton />
        </div>
    )
}

export default AboutInside